{
  "name": "etherand-contract",
  "version": "0.0.1",
  "scripts": {
    "build": "truffle migrate --reset",
    "deploy": "truffle deploy",
    "test": "truffle test",
    "lint": "solhint contracts/**/*.sol",
    "devGanache": "ganache-cli -i 5777 --account \"$(cat privateKey.example),100000000000000000000\"",
    "devBuildAndDeploy": "npm run build && truffle deploy"
  },
  "devDependencies": {
    "ganache-cli": "^6.4.1",
    "truffle": "^5.0.8"
  }
}
